//deletes the note ... learn js >:)
//js was used to basically run a page giving it noteID which told the backend
//to remove the provided noteId from the database
//
//function deleteNote(noteId) {
  //  fetch("/delete-note", {
    //  method: "POST",
    //  body: JSON.stringify({ noteId: noteId }),
    //}).then((_res) => {
    //  window.location.href = "/";
    //});
//  }
//
//r.i.p.